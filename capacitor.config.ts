import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nft-collector.app',
  appName: 'nft-collector',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
